# LLM text-extraction pipeline dependencies
# Install: pip install -r requirements_text_pipeline.txt

# LangChain stack
langchain>=0.3
langchain-anthropic>=0.3
langgraph>=0.2
langsmith>=0.1

# Vector store + embeddings (local, no API key needed)
chromadb>=0.5
sentence-transformers>=3.0

# PDF text extraction
pdfplumber>=0.11

# Data + validation
pydantic>=2.0
pandas>=2.0
openpyxl>=3.1
